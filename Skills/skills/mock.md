## Behavior

1. First confirm the company context and prd location

'AskUserQuestion' for the company context folder
'AskUserQuestion' for the prd folder

2. Read all files in the folder above.

3. Build a clickable HTML prototype for the P0 requirements.

- Single HTML file (self-contained — CSS and JS inline, no external dependencies)
- Implement sccreens based on the P0 requirements in the PRD:
- Use Persona context from company-context/user-persona.md
- Navigation between screens must work (clickable buttons/links)
- Use a clean, professional UI aesthetic, not a startu, fancy landing page

Save the file as: mocks/ads-agent-prototype.html

## Skill Metadata

- **Skill name**: `mock`
- **Trigger**: When user says "build mock", "create mock", "design mock", "mock", or similar
- **Output**: HTML prototype files in `mock/` folder
